{
  "name": "backend_express",
  "version": "1.0.0",
  "description": "\"A example web server with express\"",
  "license": "ISC",
  "author": "Michael Ramírez",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rimraf dist && cd /d \"C:/Users/USUARIO/Desktop/Aplicación/frontend_servidor/modulos\" && npm run build && cpy \"dist/**\" \"../backend_express\"",
    "deploy:full": "npm run build:ui && git add . && git commit -m \"uibuild\" && git push"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "cpy-cli": "^6.0.0",
    "nodemon": "^3.1.11",
    "rimraf": "^6.1.2"
  }
}
